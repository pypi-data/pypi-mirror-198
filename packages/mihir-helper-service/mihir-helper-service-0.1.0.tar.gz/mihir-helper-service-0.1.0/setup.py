from setuptools import setup, find_packages


setup(
    name="mihir-helper-service",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List your package's dependencies here
    ],
    entry_points={
        # Define any command-line scripts or console scripts here
    },
    # Other metadata about your package can be specified here
)
