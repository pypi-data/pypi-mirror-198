[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ezsave_shayanvd"
version = "0.0.1"
authors = [
  { name="Shayan von Düring", email="shayanvonduering@gmail.com" },
]
description = "A simple package to save python lists, dictionaries and strings to a file."
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]