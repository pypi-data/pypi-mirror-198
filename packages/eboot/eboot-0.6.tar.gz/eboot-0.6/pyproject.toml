[tool.poetry]
name = "eboot"
version = "0.6"
description = ""
authors = ["qiuyeyijian <zlw9821@qq.com>"]
readme = "README.md"

[tool.poetry.scripts]
eboot = "eboot.main:app"

[tool.poetry.dependencies]
python = "^3.7"
typer = { extras = ["all"], version = "^0.7.0" }
rich = "^12.6.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
