from setuptools import setup, find_packages

setup(
    name="evojson",
    packages=find_packages(),
    install_requires=[],
    classifiers=[
        "Programming Language :: Python :: 3.10",
    ],
)
