[tool.poetry]
name = "myapi"
version = "0.1.0"
description = "MyAPI initial description"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

packages = [
  { include="myapi", from="src" },
]

[tool.poetry.dependencies]
python = "^3.10"
bottlenest = "^0.0.15"

[tool.poetry.group.dev.dependencies]
pytest = "^7.1.2"
pytest-cov = "^3.0"

# scripts
[tool.poetry.scripts]
start = "myapi:main"
