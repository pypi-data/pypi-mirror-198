[tool.poetry]
name = "back-to-sit"
version = "0.1.0"
description = ""
authors = ["diegulio <diego.machado@usach.cl>"]
readme = "README.md"
packages = [{include = "back_to_sit"}]

[tool.poetry.dependencies]
python = "^3.10"
python-telegram-bot = "^20.1"
pyyaml = "^6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
